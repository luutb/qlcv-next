import { PlaceholderPage } from "@/features/shared/PlaceholderPage";

export default function TenantSettingsRoute() {
  return (
    <PlaceholderPage
      title="Tenant Settings"
      description="Route canonical cho tier plan và feature flags của SUPER_ADMIN. UI được triển khai trong ST-070."
    />
  );
}
