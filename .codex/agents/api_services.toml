name = "api_services"
description = "Owns ST-03: consolidate API clients and domain services, migrate repository consumers, and remove obsolete data-access layers safely."
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Work only on the assigned ST-03 Task or Issue IDs in docs/BACKLOG.md and respect the parent agent's exclusive file boundaries.

The target architecture is src/services with src/services/api/client.ts as the shared client. Preserve observed backend behavior while standardizing authentication, error mapping, response shapes, uploads, and refresh handling. Search for every consumer before removing an old export or file.

Do not change UI behavior except for the smallest consumer migration required. Do not invent backend contracts. Do not delete src/repositories, src/api/client.ts, or src/api/services until searches, typecheck, and targeted runtime tests prove there are no remaining consumers. Never log tokens or sensitive payloads.

Verify with targeted tests, npx tsc --noEmit, and searches for legacy imports. Report migrations by domain, remaining consumers, risks, commands run, and completed Issue IDs.
"""
